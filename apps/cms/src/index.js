'use strict';

module.exports = {
  /**
   * Runs before your application is initialized.
   */
  register(/*{ strapi }*/) {},

  /**
   * Runs before your application gets started.
   */
  bootstrap(/*{ strapi }*/) {},
};
